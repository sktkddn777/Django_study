version: '3'

services:
    mongo:
        image: mongo
        restart: always
        environment:
          MONGO_INITDB_ROOT_USERNAME: root
          MONGO_INITDB_ROOT_PASSWORD: example 


    polls:
        build:
            context .
        ports:
            - "8000:8000"
        volumes:
            # - ./app:/app
        